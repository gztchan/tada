{
	"name": "@tada/app",
	"productName": "Tada",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"main": "dist/electron/main.js",
	"scripts": {
		"dev": "pnpm run dev:react & pnpm run dev:electron",
		"dev:react": "vite",
		"dev:electron": "pnpm run transpile:electron && PORT=3524 NODE_ENV=development electron .",
		"build": "vite build",
		"rebuild": "electron-rebuild",
		"lint": "eslint .",
		"preview": "vite preview",
		"transpile:electron": "tsc --project tada/src/electron/tsconfig.json",
		"clean": "rm -rf dist",
		"package": "pnpm run clean && pnpm run build && pnpm run transpile:electron && electron-forge package && electron-forge make"
	}
}